module.exports.config = {
  api: {
    bodyParser: {
      sizeLimit: "10mb"
    }
  }
};

{
  "name": "food-ai-backend",
  "version": "1.0.0",
  "type": "commonjs",
  "dependencies": {
    "@google/generative-ai": "latest"
  }
}
